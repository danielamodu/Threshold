{
  "name": "@threshold/decision-layer",
  "version": "0.0.0",
  "private": true,
  "description": "Agent Decision Layer (§2) — Phase 3 hard-coded-threshold fallback. LLM orchestrator not yet built, pending §10.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -p tsconfig.check.json",
    "test": "node --import tsx --test \"test/**/*.test.ts\"",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "dependencies": {
    "@threshold/types": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
